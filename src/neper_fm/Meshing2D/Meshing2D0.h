/* This file is part of the 'neper' program. */
/* Copyright (C) 2003-2012, Romain Quey. */
/* See the COPYING file in the top-level directory. */

extern void Meshing2D (struct IN, struct GEOPARA, struct GEO, int*,
    double***, double**, double**, int*, struct NODES*, struct MESH*, struct MESH, struct
    MESH, struct NODES*, struct MESH*);
